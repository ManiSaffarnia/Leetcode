# Leetcode
Solving the Leetcodes site problems with Javascript

## Run TypeScript solution
- please run `tsc` in the root of project. After that a `dist` folder will be created inside the `solution-in-ts` folder which contains compiled codes.

- then, run `node index-ts.js`
